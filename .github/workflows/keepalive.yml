name: Keep Render Ali

on:
  schedule:
    - cron: "*/3 * * * *"   # every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render app
        run: curl -s https://n8n-app-oncloud.onrender.com/ > /dev/null
